# Animation Configuration
# Animation metadata and parameter assignments
# (Parameter definitions are in parameters.yaml)

# Animation definitions
animations:
  breathe:
    id: "breathe"
    display_name: "Breathe"
    tag: breathe
    description: "Smooth breathing effect"
    parameters: [ANIM_SPEED, ANIM_INTENSITY]  # Base (SPEED, COLOR_1) + INTENSITY

  color_fade:
    id: "color_fade"
    display_name: "Color Fade"
    tag: color_fade
    description: "Rainbow hue cycling"
    parameters: [ANIM_SPEED, ANIM_INTENSITY]  # Uses only base params (SPEED, COLOR_1 as start hue)

  snake:
    id: "snake"
    display_name: "Snake"
    tag: snake
    description: "Sequential zone chase"
    parameters: [ANIM_SPEED, ANIM_PRIMARY_COLOR_HUE, ANIM_LENGTH]  # Uses only base params (SPEED, COLOR_1)

  color_snake:
    id: "color_snake"
    display_name: "Color Snake"
    tag: color_snake
    description: "Multi-pixel rainbow snake"
    parameters: [ANIM_SPEED, ANIM_PRIMARY_COLOR_HUE, ANIM_LENGTH, ANIM_HUE_OFFSET]  # Base (ANIM_SPEED, ANIM_INTENSITY) + LENGTH + HUE_OFFSET