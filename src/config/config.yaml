# Main Configuration File
# Uses include system to load modular config files

# Include list - files loaded in order and merged
include:
  - hardware.yaml
  - zones.yaml
  - zone_mapping.yaml
  - parameters.yaml
  - animations.yaml
  - colors.yaml