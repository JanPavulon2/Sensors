/**
 * Shared Hooks
 * Cross-feature hooks for authentication, system state, etc.
 */

export { useAuth, useIsAuthenticated } from './useAuth';
export { useHealthQuery, useSystemStatusQuery, useCheckBackendConnection } from './useSystem';
